{% extends 'baseback.html.twig' %}
{% block body %}
    <div class="card-body">
    <h4 class="card-title">coach </h4>
    <div class="table-responsive">
    <div align="center">
    <a href="{{ path('addcoach') }}" class="badge badge-outline-success"  >add</a>
    </div>
    <div align="center">
    <ul class="navbar-nav w-100">
        <li class="nav-item w-100">

                <input type="text" class="form-control" placeholder="Search coach by name" >
              <button > search</button>

        </li>
    </ul>
    </div>
    <table class="table">
        <thead>
        <tr>
            <th> Id  </th>
            <th> name </th>
            <th> last name </th>
            <th> rank </th>
            <th> categorie  </th>
            <th>  reservations </th>
            <th> update  </th>
            <th> delete  </th>
        </tr>
        </thead>
        <tbody>
        <tr>
            {% for i in tabcoach %}
            <td>{{ i.id }} </td>
            <td>{{ i.name }} </td>
            <td> {{ i.lastname }} </td>
            <td> {{ i.rank }} </td>
            <td> {{ i.categorie }}  </td>
           <td>  </td>


            <td>
                <a href="{{ path('modifycoach',{'id':i.id}) }}" class="badge badge-outline-warning" >Update</a>

            </td>
            <td>
                <a href="{{ path('removecoach',{'id':i.id }) }}" class="badge badge-outline-danger" >Remove</a>

            </td>
        </tr>


        {% endfor %}
    </table>

{% endblock %}